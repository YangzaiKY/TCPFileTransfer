Git:
1. git init
2. git add --all
3. git commit
4. git remote origin Github仓库名
5. git pull origin master --allow-unrelated-histories
6. git push -u origin master

# 删除远程仓库里的文件
1. git rm --cached 文件
2. git commit -m '备注'
3. git push origin master
